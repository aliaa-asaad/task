import 'package:flutter/material.dart';

class GuideTabBar extends StatelessWidget {
  const GuideTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}